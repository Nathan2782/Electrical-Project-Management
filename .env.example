# Postgres connection string. For local dev you can point this at a local
# Postgres instance, or a free hosted one (e.g. Neon, Supabase, Vercel Postgres).
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"
